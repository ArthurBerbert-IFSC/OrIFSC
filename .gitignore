# Python compilados
__pycache__/
*.py[cod]
*.so

# SO
.DS_Store
Thumbs.db

# Artefato de build do plugin — gerado pelo workflow (qgis-plugin-ci). Nao versionar.
*.zip

# Pasta de arquivos antigos/locais — fica so na sua maquina, nao vai para o GitHub.
Antigos/
